# My-Linux-Commands

- `cat`

# Usage

```bash
git clone https://github.com/hideki1217/my-linux-commands.git

cmake -S . -B build
cmake --build build
cmake --install build --prefix ./

./bin/cat README.md
```